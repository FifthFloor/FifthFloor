using UnityEngine;
using System.Collections;

public class networkInit : MonoBehaviour {

    void Awake() 
    { /*
        if (networkView.isMine)
        {

            MouseLook m = (MouseLook)GetComponent(typeof(MouseLook));
            m.enabled = true;
            CharacterController cc = (CharacterController)GetComponent(typeof(CharacterController));
            cc.enabled = true;
            FPSInputController FPS = (FPSInputController)GetComponent(typeof(FPSInputController));
            FPS.enabled = true;
            CharacterMotor cm = (CharacterMotor)GetComponent(typeof(CharacterMotor));
            cm.enabled = true;

        }
       else
        {
            MouseLook m = (MouseLook)GetComponent(typeof(MouseLook));
            m.enabled = false;
            CharacterController cc = (CharacterController)GetComponent(typeof(CharacterController));
            cc.enabled = false;
            FPSInputController FPS = (FPSInputController)GetComponent(typeof(FPSInputController));
            FPS.enabled = false;
            CharacterMotor cm = (CharacterMotor)GetComponent(typeof(CharacterMotor));
            cm.enabled = false;
        }*/
    
    }


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
